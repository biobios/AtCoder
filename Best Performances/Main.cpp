#include <iostream>
#include <list>

using namespace std;

int main() {
	uint64_t N, K, Q;
	cin >> N >> K >> Q;
	list<uint64_t> sequence(K, 0);
	
	for (size_t i = 0; i < Q; i++) {
		uint64_t X, Y;
		cin >> X >> Y;
		auto itr = sequence.cbegin();
		while(itr.)
	}
	

	return 0;
}